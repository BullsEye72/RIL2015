<%= simple_form_for(@order,  wrapper: :horizontal_form) do |f| %>
  <%= f.error_notification %>
  <div class="form-horizontal">
    <div class="form-inputs">
      <% disabled = (action == :show) ? true : false %>
      <%#= f.input :total_due_calc, input_html: { disabled: disabled } %>
      <%= f.association :supplier, label: t('supplier').titleize, input_html: { disabled: disabled } %>
      <%= f.association :quote, label: t('quote').titleize, input_html: { disabled: disabled } %>
    </div>

    <div class="form-actions">
      <% if action == :new %>
          <%= f.submit 'Créer' %>
      <% end %>
      <% if action == :edit %>
          <%= f.submit 'Mettre à jour' %>
      <% end %>
      <%= link_to('Retour', orders_path, class: 'btn btn-default pull-right') %>
    </div>
  </div>
<% end %>